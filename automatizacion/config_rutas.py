import os

# Base del proyecto (sube solo un nivel)
ROOT_DIR = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))

# Carpetas absolutas
SCREENSHOTS_DIR = os.path.join(ROOT_DIR, "evidencia", "screenshots")
PDFS_DIR = os.path.join(ROOT_DIR, "evidencia", "pdfs")
